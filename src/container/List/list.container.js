import React from 'react';
import {FormWrapper} from '../form/form.container';
class TodoList extends React.Component {
    constructor(props){
        super(props);
    }

    render(){
        return <FormWrapper />
    }
}

export {TodoList}